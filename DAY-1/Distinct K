#include <iostream>
#include <map>
#include <vector>
using namespace std;

int main() {
    int N;
    cin >> N;
    
    map<string, int> freq; // To store frequency of each string
    vector<string> order;  // To store order of first appearances
    
    for (int i = 0; i < N; i++) {
        string s;
        cin >> s;
        
        if (freq[s] == 0) {
            order.push_back(s); // Store first occurrence
        }
        freq[s]++; // Increment frequency
    }

    int k;
    cin >> k;

    int uniqueCount = 0;
    for (const string &s : order) {
        if (freq[s] == 1) { // If it's unique
            uniqueCount++;
            if (uniqueCount == k) {
                cout << s << endl;
                return 0;
            }
        }
    }

    cout << "-1" << endl; // If less than k unique strings exist
    return 0;
}
